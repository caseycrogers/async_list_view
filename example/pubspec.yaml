name: example
description: An example application using AsyncListView

publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  async_list_view: 0.0.1-dev.3

  cupertino_icons: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
